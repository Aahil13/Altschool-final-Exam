version: "3"
services:
  web:
    build: .
    depends_on:
      - mongodb
    ports:
      - "80:80"
    environment:
      MONGO_HOST: localhost
      MONGO_PORT: 27017
      MONGO_DB: firstmongo
  mongodb:
    image: mongo:latest
    volumes:
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
